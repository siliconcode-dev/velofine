// Shared cross-engine utilities (config system, logging, GPU/hardware detection).
// Configuration inherited from the root project's `subprojects` block.

dependencies {
    implementation("com.google.code.gson:gson:2.14.0")
}
